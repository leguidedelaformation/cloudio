{% extends "CdoBlogBundle:Admin:layout.html.twig" %}

{% block title %}
    Supprimer la page &laquo {{ page.title }} &raquo
{% endblock title %}

{% block heading %}
    <h1>Supprimer la page &laquo {{ page.title }} &raquo</h1>
{% endblock heading %}

{% block innercontent %}
    <br />
    <p class="text-center">Etes-vous certain(e) de vouloir supprimer la page &laquo; <b>{{ page.title }}</b> &raquo; ?</p>
    <p class="text-center">Vous ne pourrez pas revenir sur cette action.</p>
    <br />
    <form action="{{ path('cdo_blog_admin_page_remove', {'page': page.id, 'subdomain': app.session.get('subdomain')}) }}" method="post" class="text-center">
        <a href="{{ app.request.headers.get('referer') }}" class="btn btn-default btn-lg">Annuler</a>
        <input type="submit" value="Supprimer" class="btn btn-danger btn-lg" />
        {{ form_rest(form) }}
    </form>
{% endblock innercontent %}