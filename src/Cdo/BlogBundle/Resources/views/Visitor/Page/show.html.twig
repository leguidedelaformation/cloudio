{% extends "CdoBlogBundle:Visitor/Page:layout" ~ placement ~ ".html.twig" %}

{% block page_content %}
    <div id="pagecontent">
        <h1>{{ page.title }}</h1>
        <p>{{ page.content|raw }}</p>
    </div><!-- /#pagecontent -->
{% endblock page_content %}